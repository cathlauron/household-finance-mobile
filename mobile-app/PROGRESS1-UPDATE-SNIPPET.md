0. **IN PROGRESS: Checkpoint A.7.9 (automated testing setup) — Maestro flow now passes end-to-end; surfaced a real account-creation bug on the emulator.** All four pieces (Android emulator, local Firebase emulator, Expo, and a command terminal) confirmed running together successfully. `flows/create-profile.yaml` updated with an `optional: true` tap to auto-dismiss a harmless Expo-notifications warning box that was previously blocking the final "Create profile" tap. After also working around a one-time Android system "stylus" popup and re-wiping app data between attempts (`adb shell pm clear host.exp.exponent`), got a fully clean run: every step in the flow passed, including the final button tap.

   **NEW BUG FOUND (blocks all further testing until fixed):** once the "Create profile" button is actually reached and tapped, the app shows "Something went wrong creating your account. Check your internet connection and try again." — this happens on the emulator despite the app's own logs confirming `🧪 Connected to LOCAL Firebase emulator` earlier in the same session. Not yet investigated. This must be root-caused and fixed before any further checklist items can be tested.

   Also built and saved `EMULATOR-TESTING-CHECKLIST.md` at the repo root — a full, section-by-section checklist covering every screen, button, and setting in the app (auth/PIN/linking, all 10+ tabs, all Settings sections, and app-wide/cross-cutting items), to be worked through once the account-creation bug is resolved and before moving to real-device testing.

   NEXT ACTION: investigate the account-creation failure (likely candidates to check first: whether the emulator's `10.0.2.2` Firebase-emulator address is actually being reached correctly from inside the Android emulator at the moment "Create profile" is tapped, or whether this is a genuine app-code bug in the sign-up flow itself, unrelated to the emulator setup). Once fixed, begin working through `EMULATOR-TESTING-CHECKLIST.md` from the top. IMPORTANT REMINDER: `USE_FIREBASE_EMULATOR` is still `true` in `firebase.ts` — must be set back to `false` before any real-account (non-testing) use.
